# Puerto del servidor
server.port=${PORT:8080}

# Conexión directa a Railway 
spring.datasource.url=jdbc:mysql://tramway.proxy.rlwy.net:31813/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=EhPXnfZNNMrziakPTsYWhEwFoCpImJEj
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.use_sql_comments=true

# Codificación de caracteres
spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci

# Configuración de logs SQL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.hibernate.tool.hbm2ddl=DEBUG

# Clave API de OpenWeather (esto no afecta la BD)
openweather.api.key=${OPENWEATHER_API_KEY:5c030daea748a0efd41401d1a9835666}
